

CREATE COMPUTE MODULE TASK_DECRYPT_PAYLOAD_MF_Compute
	CREATE FUNCTION Main() RETURNS BOOLEAN
	BEGIN
		SET OutputRoot.BLOB.BLOB = DECRYPT_DATA(InputRoot.BLOB.BLOB);
	END;
END MODULE;

CREATE PROCEDURE DECRYPT_DATA (IN INPUT BLOB ) RETURNS BLOB 
LANGUAGE JAVA
EXTERNAL NAME "decryption.Decrypt.decryptData";
