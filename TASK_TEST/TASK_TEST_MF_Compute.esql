

CREATE COMPUTE MODULE TASK_TEST_MF_Compute
	CREATE FUNCTION Main() RETURNS BOOLEAN
	BEGIN
		DECLARE INTA INTEGER;
		SET INTA = CAST(InputRoot.XMLNSC.Data.intA AS INTEGER);
		DECLARE INTB INTEGER;
		SET INTB = CAST(InputRoot.XMLNSC.Data.intB AS INTEGER);
		
		SET OutputRoot.XMLNSC.Data.Result = INTA + INTB;
	END;
END MODULE;
